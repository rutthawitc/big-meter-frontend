export default function App() {
  return (
    <div className="min-h-screen bg-gray-50 text-slate-900">
      <main className="container mx-auto px-6 py-12">
        <h1 className="text-3xl font-bold">Big Meter</h1>
        <p className="mt-2 text-slate-600">React + Tailwind starter (minimal reset).</p>
        <div className="mt-8 rounded-lg border border-slate-200 bg-white p-6">
          <p>Ready to build the new Detail page.</p>
          <ul className="mt-4 list-disc pl-6 text-slate-700">
            <li>Pure React + TailwindCSS</li>
            <li>No routing, no query libs yet</li>
          </ul>
        </div>
      </main>
    </div>
  )
}
